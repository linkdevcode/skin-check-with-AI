# Vercel Postgres — copy to .env.local for local dev
# Lấy connection string từ Vercel Dashboard → Storage → Postgres → .env.local
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
